apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: env-green-apps
  namespace: argo-cd
spec:
  generators:
    - git:
        repoURL: https://github.com/ekalabs/crossplane-vcluster.git
        revision: HEAD
        directories:
          - path: crossplane-manifests/env-green/apps/*/*
  template:
    metadata:
      name: green-{{path.basename}}
    spec:
      project: default
      source:
        repoURL: https://github.com/ekalabs/crossplane-vcluster.git
        targetRevision: HEAD
        path: '{{path.path}}'
      destination:
        server: https://env-green.env-green:443
        namespace: crossplane-system
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
