---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: env-preview-crossplane-providers
  namespace: argo-cd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  labels:
    clusterName: env-preview
spec:
  project: default
  sources:
    - path: platform-infra/clusters/preview/crossplane-providers
      repoURL: https://github.com/ekalabs/crossplane-vcluster.git
      targetRevision: HEAD
  
  destination:
    server: https://env-preview.env-preview:443
    namespace: crossplane-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 30s
        factor: 5
        maxDuration: 3m0s

