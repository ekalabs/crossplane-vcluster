apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: env-prod-apps
  namespace: argo-cd
spec:
  generators:
    - matrix:
        generators:
          - merge:
              mergeKeys:
                - service
              generators:
                - git:
                    repoURL: https://github.com/ekalabs/crossplane-vcluster.git
                    revision: main
                    files:
                      - path: environment-config/prod/services/*.yaml
                - git:
                    repoURL: https://github.com/ekalabs/crossplane-vcluster.git
                    revision: main
                    files:
                      - path: promotion-repo/prod/*.yaml
          - clusters:
              selector:
                matchLabels:
                  argocd.argoproj.io/secret-type: cluster
                  clusterName: env-prod
  template:
    metadata:
      name: prod-{{appName}}
      labels:
        clusterName: env-prod
    spec:
      project: default
      source:
        repoURL: https://github.com/ekalabs/crossplane-vcluster.git
        targetRevision: main
        path: '{{servicePath}}/k8s'
      destination:
        server: '{{server}}'
        namespace: '{{namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
