apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: env-qa-apps
  namespace: argo-cd
spec:
  generators:
    - git:
        repoURL: https://github.com/ekalabs/crossplane-vcluster.git
        revision: HEAD
        directories:
          - path: apps/*/*
  template:
    metadata:
      name: qa-{{path.basename}}
      labels:
        clusterName: env-qa
    spec:
      project: default
      source:
        repoURL: https://github.com/ekalabs/crossplane-vcluster.git
        targetRevision: HEAD
        path: '{{path}}/k8s'
      destination:
        server: https://env-qa.env-qa:443
        namespace: crossplane-system
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
