---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: env-management-crossplane-manifests
  namespace: argo-cd
  annotations:
    argocd.argoproj.io/sync-wave: "4"
  labels:
    clusterName: env-management
spec:
  project: default
  sources:
    - path: crossplane-manifests/env-management
      repoURL: https://github.com/ekalabs/crossplane-vcluster.git
      targetRevision: HEAD

  destination:
    server: https://env-management.env-management:443
    namespace: crossplane-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 30s
        factor: 5
        maxDuration: 3m0s

