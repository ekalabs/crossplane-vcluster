---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: env-management-app
  namespace: argo-cd
  labels:
    clusterName: env-management
spec:
  project: default
  sources:
    - path: environments/env-management
      repoURL: https://github.com/ekalabs/crossplane-vcluster.git
      targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: argo-cd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

